# Jour 14 - Typage moderne en Python

**Date** : 2026-07-30
**Durée effective** : 6h00

---

# Ce que j'ai appris

Aujourd'hui, j'ai appris à utiliser le typage moderne en Python grâce aux **annotations de types** (*type hints*).

J'ai compris que les annotations de types ne modifient pas l'exécution du programme, mais qu'elles permettent :

* d'améliorer la lisibilité du code ;
* de mieux documenter les fonctions et les classes ;
* de détecter certaines erreurs avant l'exécution grâce à des outils comme `mypy`.

J'ai travaillé avec les types de base :

* `str`, `int`, `float`, `bool` ;
* `list`, `dict`, `set`, `tuple` ;
* les valeurs optionnelles avec `str | None`.

J'ai également découvert plusieurs notions avancées du typage en Python :

* `TypedDict` pour définir des dictionnaires avec une structure précise ;
* `TypeVar` pour créer des fonctions génériques ;
* `Generic[T]` pour créer des classes réutilisables avec différents types ;
* `Protocol` pour définir des contrats que certaines classes doivent respecter ;
* `Self` pour typer les méthodes qui retournent une instance de leur propre classe.

J'ai appris à configurer et utiliser `mypy` en mode strict afin de vérifier automatiquement la qualité du typage dans un projet Python.

J'ai également compris la différence entre :

* un générique (`Repository[T]`), qui permet de créer du code réutilisable avec plusieurs types ;
* un protocole (`Serializable`), qui impose un comportement commun à plusieurs classes sans nécessiter d'héritage direct.

---

# Ce que j'ai codé

## Exercices du cours

### `bibliotheque.py`

J'ai créé un module de gestion d'une bibliothèque entièrement typé.

Le script permet :

* de créer une structure de livres avec `TypedDict` ;
* d'ajouter, rechercher, supprimer et emprunter des livres ;
* de typer les paramètres, les valeurs de retour des fonctions ainsi que les variables ;
* de vérifier la qualité du typage avec `mypy --strict`.

### `pyproject.toml`

J'ai configuré `mypy` en mode strict.

La configuration comprend :

* Python 3.12 ;
* l'activation de `strict = true` ;
* la vérification automatique du typage du projet.

### `repository.py`

J'ai développé un système de repository générique utilisant les fonctionnalités avancées du typage Python.

Le script contient :

* une classe générique `Repository[T]` capable de stocker différents types d'objets ;
* les méthodes `add()`, `get()`, `list()` et `delete()` ;
* un protocole `Serializable` définissant les méthodes `to_dict()` et `from_dict()` ;
* les classes `Person` et `Product` qui implémentent ce protocole ;
* une classe `Animal` utilisée pour tester les erreurs de typage ;
* une fonction `save_to_json()` permettant d'enregistrer en JSON tous les objets compatibles avec `Serializable`.

---

## Projet personnel : **Sini**

En parallèle de mes cours, j'ai poursuivi le développement de mon projet personnel **Sini**, une application de gestion agricole développée avec FastAPI.

L'objectif de cette séance était de mettre en place les **schémas Pydantic** afin de garantir la validation des données échangées par l'API.

### `user.py`

J'ai développé les modèles liés aux utilisateurs :

* création de l'énumération `RegionMali` regroupant les différentes régions du Mali ;
* création des modèles `UserBase`, `UserCreate`, `UserUpdate` et `UserResponse` ;
* validation des numéros de téléphone maliens à l'aide d'une expression régulière et d'un `field_validator` ;
* optimisation de la compilation de la regex grâce à `lru_cache` ;
* utilisation de `ConfigDict(from_attributes=True)` pour faciliter la conversion des modèles SQLAlchemy vers les schémas Pydantic.

### `parcelle.py`

J'ai créé les schémas représentant les parcelles agricoles.

Ils permettent de :

* définir les différentes cultures avec l'énumération `CultureType` ;
* créer les modèles `ParcelleBase`, `ParcelleCreate`, `ParcelleUpdate` et `ParcelleResponse` ;
* valider les informations concernant la superficie, la culture, la région, la commune et le propriétaire.

### `journal.py`

J'ai développé les schémas du journal des activités agricoles.

Ils permettent de :

* définir les différents types d'actions (`ActionType`) ;
* enregistrer les interventions réalisées sur une parcelle ;
* valider les informations telles que le titre, la description, le coût et le type d'intervention.

### `diagnostic.py`

J'ai conçu les schémas dédiés aux diagnostics agricoles.

Ils permettent de :

* représenter les niveaux de gravité avec `SeverityLevel` ;
* créer les modèles `DiagnosticBase`, `DiagnosticCreate` et `DiagnosticResponse` ;
* valider les symptômes observés, les recommandations, le score de confiance du modèle d'intelligence artificielle ainsi que le niveau de sévérité.

### `__init__.py`

J'ai centralisé l'export de tous les schémas Pydantic afin de simplifier les importations dans le reste du projet.

Ce travail constitue une étape importante dans l'architecture de **Sini**, car il garantit une validation fiable des données avant leur traitement par les futures routes FastAPI et les modèles de base de données.

---

# Ce qui m'a bloqué

J'ai rencontré plusieurs difficultés lors de l'utilisation des fonctionnalités avancées du typage Python avec `mypy`.

Les principales difficultés étaient :

* la différence entre `list[Serializable]` et `Sequence[Serializable]` ;
* la compréhension de l'invariance des listes en Python ;
* l'utilisation correcte des `Protocol` ;
* le typage des dictionnaires avec `dict[str, object]` ;
* l'utilisation de `cast()` pour aider `mypy` à déduire certains types ;
* la compréhension du rôle de `TypeVar` et de `Generic[T]`.

J'ai également dû comprendre pourquoi certaines classes étaient acceptées par `Repository[T]` mais refusées par `save_to_json()`.

---

# Ressources et points d'approfondissement

* https://peps.python.org/pep-0484/
* https://mypy.readthedocs.io/
* https://docs.python.org/3/library/typing.html
* https://github.com/microsoft/pyright
* https://mypy.readthedocs.io/en/stable/cheat_sheet_py3.html

---

# Ce que je veux revoir

Je souhaite revoir :

* les différences entre `Protocol`, l'héritage classique et le *duck typing* ;
* les génériques avec `TypeVar` et `Generic[T]` ;
* la covariance et l'invariance des collections Python ;
* les différents outils du module `typing` ;
* les messages d'erreur de `mypy` afin de pouvoir les comprendre et les corriger plus rapidement.

---

# Note d'humeur

Cette journée a été très enrichissante.

Les exercices du cours m'ont permis d'approfondir le typage moderne en Python et de mieux comprendre les fonctionnalités avancées proposées par `typing` et `mypy`.

En parallèle, j'ai pu appliquer directement ces connaissances dans mon projet **Sini** en concevant des schémas Pydantic robustes et bien structurés. Cette mise en pratique m'a permis de consolider les notions vues en cours tout en faisant progresser un projet concret.

Je constate que le typage apporte une réelle valeur au développement d'une API : il améliore la qualité du code, facilite la maintenance et permet de détecter de nombreuses erreurs dès la phase de développement.
